const DEFAULT_SETTINGS = {
  incognitoMode: false,
  autoClose: true,
  maxResults: 10,
  extractTimeout: 10,
  extractDelay: 3000,
  urlPatterns: ["http://127.0.0.1:*/*", "http://localhost:*/*"],
};
